Publish prep: stop the self-test polluting the provenance store; correct a false claim about -dirty stamps

CI-34 -- the gate's self-test wrote a manifest holding a deliberately fake
number (SelfTestManifested = 0.4242) to prove the gate resolves a manifested
value, and never removed it. Forty accumulated: 61% of the live provenance
store, every one recording an uncommitted tree, and one of them leaking the
fake number into paper/numbers.tex, the generated file the manuscript reads
from. Nothing ever cited them and no manuscript sentence used the macro, so
nothing published was wrong. The selftest now deletes its fixture and reindexes
before returning; the forty are retired to superseded/. numbers.tex: 564 -> 563.

CI-35 -- the previous round asserted that a "-dirty" commit stamp is "not
resolvable from the public repository", and that wording propagated into
HUMAN_ACTIONS.md, PUBLISH_INSTRUCTIONS.md and the operator's next action. It is
false. A -dirty stamp is <sha>-dirty and the base sha resolves normally; all
twelve distinct base commits recorded across every live manifest resolve and are
ancestors of the pushed branch. What -dirty records is narrower: the tree
carried uncommitted edits at run time, so the exact source state is not
recoverable. Corrected in all four places. The check's VERDICT is unchanged --
it still exits 1 -- because correcting a false rationale is not relaxing a
threshold.

Publish-ready status after this commit: tree clean, HEAD published, and 17 live
manifests still carry the uncommitted-tree marker. Two of those are irreducible:
the CICIDS construction-contrast arms ran 4.8 hours on an AWS Linux instance
that no longer exists, and re-running them on this Windows machine would change
published numbers -- the cross-platform effect already recorded as CI-16. The
remaining fifteen could be re-run here but were not, because doing so on a round
whose brief forbids content changes risks altering published values for a
provenance tidy-up. That is a judgement, recorded as such in
PUBLISH_PREP_REPORT.md, and it is the operator's to overturn.

Verification: gate 8/8 exit 0, 563 macros, ledger 31 rows, 71 derived quantities
consistent, no overfull, 8 staged artifacts fresh, both builds 3-pass exit 0
with 0 undefined at 17 pp, 72 tests. Documented reproduction instructions
executed inside both the DTRAP and Zenodo extractions: all exit 0.

Nothing has been deposited, submitted or sent.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
